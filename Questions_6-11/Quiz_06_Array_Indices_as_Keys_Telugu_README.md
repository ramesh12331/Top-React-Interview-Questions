# ⚛️ React Interview Q6 — Array Indices ను Keys గా వాడితే ఏమవుతుంది?

## ✅ ప్రశ్న
**React లో array indices ను keys గా వాడితే ఏమవుతుంది?**

---

## 🔍 Concept Recap
React లో **keys** అనేవి Virtual DOM కి ప్రతి element‌ను గుర్తించడానికి ఉపయోగపడతాయి.  
ఇవి React కి ఏ elements కొత్తవో, ఏవి మారాయో, ఏవి తొలగించబడ్డాయో తెలుసుకోవడంలో సహాయపడతాయి.

మీరు **array indices** (`0, 1, 2...`) ను keys గా వాడితే, React లిస్ట్ reorder అయ్యినప్పుడు elements‌ను తప్పుగా match చేస్తుంది.  
దీని వల్ల UI లో అనుకోని ప్రవర్తన (unexpected behavior) జరుగుతుంది.

---

## ⚠️ సమస్య ఉదాహరణ — Index ను Key గా వాడటం
```jsx
const items = ['Apple', 'Banana', 'Cherry'];

function FruitList() {
  return (
    <ul>
      {items.map((item, index) => (
        <li key={index}>{item}</li> // ❌ తప్పు: index ను key గా వాడటం
      ))}
    </ul>
  );
}
```

### Array order మారితే:
```js
['Banana', 'Apple', 'Cherry']
```

React అనుకుంటుంది:
- Index 0 → “Apple” అని, కానీ ఇప్పుడు “Banana” ఉంది  
- Index 1 → “Banana” అని, కానీ ఇప్పుడు “Apple” ఉంది  

⚡ **ఫలితం:** React పాత DOM nodes ను reuse చేస్తుంది — దీని వల్ల  
- UI తప్పుగా కనిపిస్తుంది  
- Inputs లో values తప్పుగా వస్తాయి  
- Animations తప్పుగా ఉంటాయి  

---

## 🧠 బగ్ ఉదాహరణ
```jsx
const [fruits, setFruits] = useState(['Apple', 'Banana', 'Cherry']);

return (
  <ul>
    {fruits.map((fruit, index) => (
      <input
        key={index}
        value={fruit}
        onChange={e => {
          const newFruits = [...fruits];
          newFruits[index] = e.target.value;
          setFruits(newFruits);
        }}
      />
    ))}
  </ul>
);
```

మీరు లిస్ట్ మొదట్లో కొత్త item చేర్చితే,  
👉 **input values తప్పు ఫీల్డ్స్‌లో కనిపిస్తాయి** 😱  

---

## ✅ సరైన పద్ధతి — Unique IDs వాడటం
```jsx
const fruits = [
  { id: 101, name: 'Apple' },
  { id: 102, name: 'Banana' },
  { id: 103, name: 'Cherry' }
];

function FruitList() {
  return (
    <ul>
      {fruits.map(fruit => (
        <li key={fruit.id}>{fruit.name}</li> // ✅ సరైన పద్ధతి
      ))}
    </ul>
  );
}
```

ఇప్పుడు లిస్ట్ order మారినా, React ప్రతి element ని సరైన DOM node కి సరిపెడుతుంది.

---

## 💡 సంక్షిప్త వివరణ (Short Summary)

| Concept | వివరణ |
|----------|--------|
| **ఏం జరుగుతుంది** | React DOM nodes ను తప్పుగా reuse చేస్తుంది |
| **ఎందుకు** | Array indices order మారినప్పుడు మారిపోతాయి |
| **ఫలితం** | UI bugs, తప్పు data, animations సమస్యలు |
| **సరైన పద్ధతి** | Unique IDs ను keys గా వాడటం |
| **ఎప్పుడు OK** | లిస్ట్ static గా ఉండి, ఎప్పుడూ మారకపోతే మాత్రమే |

---

## 🧠 Interview Tip
> "React లిస్ట్ items ను గుర్తించడానికి keys వాడుతుంది.  
> Array index ను key గా వాడితే, order మారినప్పుడు UI తప్పుగా update అవుతుంది.  
> కాబట్టి ఎల్లప్పుడూ unique మరియు stable IDs ను key గా వాడాలి." ✅
