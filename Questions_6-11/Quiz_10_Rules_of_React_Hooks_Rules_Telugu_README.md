# 🧠 React Hooks నియమాలు

React లో **Hooks** అనేవి ప్రత్యేకమైన ఫంక్షన్లు — వీటి ద్వారా functional components కూడా **state** మరియు **lifecycle** లక్షణాలను (`useState`, `useEffect`, `useContext`) వాడగలవు.

React హుక్ కాల్స్ క్రమాన్ని (order) ట్రాక్ చేస్తుంది. కాబట్టి హుక్‌లు సరిగ్గా పని చేయాలంటే కొన్ని **కఠినమైన నియమాలు** పాటించాలి.

ఈ నియమాలను ఉల్లంఘిస్తే, React ఏ state ఏ హుక్‌కు చెందినదో గుర్తించలేకపోతుంది — ఫలితంగా బగ్స్ వస్తాయి.

---

## ⚙️ React Hooks యొక్క 3 ముఖ్యమైన నియమాలు

### 1️⃣ హుక్‌లను ఎప్పుడూ టాప్ లెవెల్‌లోనే కాల్ చేయాలి

**✅ అర్థం:**  
Hooks‌ను ఎప్పుడూ component లేదా custom hook లో **టాప్ లెవెల్‌లోనే** కాల్ చేయాలి — loops, conditions లేదా nested functions లో కాదు.

**💡 ఎందుకు:**  
React state మరియు effects‌ను క్రమం ప్రకారం ట్రాక్ చేస్తుంది. ఆ క్రమం మారితే React state‌ను సరైన హుక్‌తో మ్యాచ్ చేయలేకపోతుంది.

**❌ తప్పు ఉదాహరణ**
```jsx
function MyComponent({ isLoggedIn }) {
  if (isLoggedIn) {
    const [user, setUser] = useState("Guest"); // ❌ షరతులో వాడటం తప్పు
  }
  return <div>Welcome!</div>;
}
```

**✅ సరైన ఉదాహరణ**
```jsx
function MyComponent({ isLoggedIn }) {
  const [user, setUser] = useState("Guest"); // ✅ ఎల్లప్పుడూ టాప్‌లో

  if (isLoggedIn) {
    console.log("User logged in!");
  }

  return <div>Hello {user}</div>;
}
```

---

### 2️⃣ హుక్‌లను React functions లో మాత్రమే కాల్ చేయాలి

**✅ Hooks వాడదగిన చోటు:**
- React functional components లో
- Custom hooks లో (`use` తో మొదలయ్యేవి)

**🚫 వాడకూడని చోటు:**
- సాధారణ JavaScript functions
- Class components
- Event handlers, loops, లేదా nested functions

**❌ తప్పు ఉదాహరణ**
```jsx
function fetchData() {
  const [data, setData] = useState([]); // ❌ Error!
}
```

**✅ సరైన ఉదాహరణ**
```jsx
function MyComponent() {
  const [data, setData] = useState([]);
  return <p>Data length: {data.length}</p>;
}
```

లేదా custom hook లో:
```jsx
function useFetchData() {
  const [data, setData] = useState([]);
  return { data, setData };
}
```

---

### 3️⃣ Custom Hooks “use” తో ప్రారంభం కావాలి

**✅ ఉదాహరణ:**
```jsx
function useUserData() {
  const [user, setUser] = useState(null);
  return [user, setUser];
}
```

**🚫 తప్పు:**
```jsx
function getUserData() { // ❌ “use” తో మొదలు కాలేదు
  const [user, setUser] = useState(null);
}
```

ఈ naming నియమం వల్ల React మరియు ESLint హుక్‌లను గుర్తించగలవు.

---

## 🧩 React లో ఇది ఎందుకు అవసరం?

React హుక్‌లను ఒక **linked list** రూపంలో ట్రాక్ చేస్తుంది.

```
Hook 1 → Hook 2 → Hook 3 → Hook 4 ...
```

ఆ క్రమం మారితే React mapping తప్పిపోతుంది.

**❌ తప్పు ఉదాహరణ**
```jsx
function Example({ show }) {
  if (show) {
    const [count, setCount] = useState(0); // ❌ తప్పు
  }
  const [name, setName] = useState("Ramesh");
  return <p>{name}</p>;
}
```

**✅ సరైన ఉదాహరణ**
```jsx
function Example({ show }) {
  const [count, setCount] = useState(0);
  const [name, setName] = useState("Ramesh");

  if (!show) return <p>Hidden</p>;

  return <p>{name} — Count: {count}</p>;
}
```

---

## 🧩 ESLint తో ఆటోమేటిక్ చెక్

```bash
npm install eslint-plugin-react-hooks --save-dev
```

**`.eslintrc` లో:**
```json
{
  "plugins": ["react-hooks"],
  "rules": {
    "react-hooks/rules-of-hooks": "error",
    "react-hooks/exhaustive-deps": "warn"
  }
}
```

---

## 💡 ఇంటర్వ్యూ ప్రశ్నలు

**Q1:** హుక్‌లను టాప్ లెవెల్‌లో ఎందుకు కాల్ చేయాలి?  
➡ React హుక్ కాల్స్ క్రమాన్ని ఆధారంగా state‌ను ట్రాక్ చేస్తుంది.

**Q2:** హుక్‌లను callback లేదా event handler లో వాడవచ్చా?  
➡ కాదు, అవి render సమయంలో మాత్రమే వాడాలి.

**Q3:** Custom hooks “use” తో ఎందుకు ప్రారంభం కావాలి?  
➡ React మరియు ESLint వాటిని సులభంగా గుర్తించడానికి.

---

## 🧾 సారాంశ పట్టిక

| నియమం | వివరణ | ఉదాహరణ |
|--------|--------|----------|
| 1️⃣ Top-level only | loops, conditions లో వాడరాదు | ✅ టాప్‌లో useState() |
| 2️⃣ Only inside React | React component లేదా hook లో మాత్రమే | ❌ సాధారణ JS function లో కాదు |
| 3️⃣ Must start with “use” | Custom hook పేరు “use” తో మొదలు కావాలి | ✅ useFetchData() |

---

## 🧭 సులభంగా గుర్తుంచుకోండి

Hooks అనేవి React roller coaster లో సీట్లు 🎢 లాంటివి.  
మీరు ఒక సీటు (hook call) skip చేస్తే, React తప్పు సీటుకు state ఇస్తుంది!

✅ ఎల్లప్పుడూ ఒకే క్రమంలో కాల్ చేయండి  
✅ React functions లో మాత్రమే వాడండి  
✅ Custom hooks ఎల్లప్పుడూ “use” తో మొదలు కావాలి